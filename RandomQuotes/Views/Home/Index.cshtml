@model RandomQuotes.Models.Quote

@{
    ViewData["Title"] = "Random Quotes";
}

<h2>Quotes as a Service</h2>

<div class="panel panel-default">
    <div class="panel-body">
        <blockquote>
            <p>
                @Model.QuoteText
            </p>
            <footer>@Model.Author</footer>
        </blockquote>
    </div>
</div>

<div class="btn btn-success btn-lg text-center">
    <a style="color:white" href="@Url.Action("Index")"><span>Refresh</span></a>
</div>
